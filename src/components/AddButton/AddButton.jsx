import Container from './AddButton.styled';

const AddButton = () => {
    return (
        <Container>
            <button>
                Add contact
            </button>
        </Container>
    )
};

export default AddButton;